# Database Configuration
DB_HOST=seu_host_mysql
DB_USER=seu_usuario
DB_PASS=sua_senha
DB_NAME=nome_do_banco

# JWT Configuration (gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=seu_jwt_secret_aqui_deve_ser_muito_longo_e_aleatorio

# Server Configuration
PORT=3000

# Frontend URL para CORS (em produção, use a URL real)
FRONTEND_URL=http://localhost:3000
